# GraphFin 0.1.0-alpha — draft

This alpha brings the multi-graph performance and native time-series work streams
together in a general-purpose graph database derived from TuGraph 4.5.2. It is
intended for evaluation of the explicitly qualified feature set.

## Included work

- Vertex and edge time-series fields with typed measures, point/range reads,
  updates, value-based compare-and-set and aggregates.
- Lazy graph loading, bounded open-graph admission and eviction infrastructure.
- Existing HA replication implementation and dedicated merged-series tests.
- Whole-graph placement/routing/fencing/migration-state components, with the
  unfinished data-plane integration identified as experimental.
- English and Chinese product READMEs, a combined roadmap, generated feature
  artwork, and a merge qualification plan using finance and telemetry fixtures.

## Compatibility and known limits

Product version 0.1.0-alpha is separate from the inherited engine compatibility
version 4.5.2. Legacy executable and API names remain. There is no claim of an
upstream-to-fork in-place upgrade, downgrade or mixed-version HA support until the
corresponding fixture/restore tests pass.

Series currently hold one mutable point per timestamp. Historical revisions,
knowledge-time context, bounded cursors and native batch import remain planned.
New non-finite DOUBLE writes are rejected; previously stored non-finite observations
need an explicit export/migration policy. Value-based CAS is not request deduplication.
Live shard forwarding, server fence integration and migration data movement are
not completed features.

## Validation and artifacts

Frozen candidate `5e31bacf50c2e1c0c2a9f6916f3a2c80f6ea46bc`
(branch `perf-series-merge`, empty dirty manifest), built with
`ci/phase0/build.sh` (`CLEAN=0`, incremental; engine-fix rebuild in
`phase0-logs/build-20260922-185044.log`) under compile image
`sha256:2350a9a1f998b6898b169d28015c60c49b488def8980b9553a657819763afedf`
(CentOS 7.9, GCC 8.4.0, `RelWithDebInfo`). Strict isolated gates
(`bash ci/merge/run.sh {unit,smoke,clients,ha}`), all with zero failures and
zero required skips:

- unit 110/110 — evidence `/tmp/graphfin-merge-results/unit-7aprcjji`
  (includes `TestSeriesTransaction.MutatingProcedureCallsWithReturnClassifyAsWrites`);
  `unit_test`
  `3e3d33a349ec17a75e1e2649bbde65ceb8feca79545dad92439a0910808a0e38`.
- smoke 4/4 (joint financial/telemetry fixtures) — evidence
  `/tmp/graphfin-merge-results/smoke-sh_x0e7u`.
- clients 14/14 with `neo4j==4.4.6` (dependency bundle `f9679a33…`) —
  evidence `/tmp/graphfin-merge-results/clients-5odtbvuq`.
- HA series failover 2/2: exact per-replica series reconciliation after
  leader loss and restart — evidence
  `/tmp/graphfin-merge-results/ha-2k7e6e4c`.
  Shared server binary `lgraph_server`
  `5010f5ecabbefbe3162483c500e8c06c326152fc95a4a21e721fafd40485d178`.

Each evidence directory holds `manifest.json` (source commit, dirty manifest,
binary hashes, image ID, command, timestamps), `results.xml`, `run.log`, and
(for HA/smoke) per-node server logs. `python3 ci/release/check_docs.py`
passes; `git diff --check` is clean.

Packages and the GitHub prerelease are not yet built or published. A local
development runtime image was built from these binaries for connecting
applications and pushed to Docker Hub as `zziangz/graphfin:0.1.0-alpha`
(also tagged `zziangz/graphfin:5e31bacf`, digest
`sha256:5eb1327977caff7ab02d53c3a04e7465dc1a122e13dd8aa40f16d899aadb46f8`,
linux/arm64, entrypoint runs `lgraph_server` with HTTP 7071 / RPC 9091 /
Bolt 7687 and data volume `/data`); it was smoke-tested (startup, graph
creation, series DDL/append/range over RPC). Registry publication of
versioned release artifacts beyond this dev image still follows
post-release workstreams D–F; do not publish this draft as
evidence that the GitHub prerelease has happened.
