name: code-scan
on:
  push:
    branches:
      - main
  workflow_dispatch:

# Disabled placeholder: the previous Alipay Cloud DevOps codescan required
# Aliyun credentials (ALI_PID / ALI_PK) that this repository does not have.
# Keep the workflow file so the intent is visible, but do nothing until a
# scan environment for ziangziangziang/graphfin is provisioned.
jobs:
  scan:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "code scan disabled: no scan environment provisioned for this repository."'
